import React from 'react';
import { Link } from 'react-router';

const Footer = (props) => {
    return (
        <footer className="footer">
            底部
        </footer>
    )
};

export default Footer;